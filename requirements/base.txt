Django>=5.1,<6.0
psycopg[binary]>=3.1
# Pinado: templates/admin/nav_sidebar.html copia o markup interno do Unfold
# (drawer + backdrop mobile). A partir de 0.88 o Unfold reescreveu a sidebar
# (sidebarOpen + largura dinâmica) — incompatível com o override, faz a sidebar
# sobrepor o conteúdo. Ao atualizar, re-sincronizar o override antes de subir o pin.
django-unfold==0.97.0
django-htmx>=1.17
Pillow>=10.0
django-imagekit>=5.0
whitenoise>=6.5
django-environ>=0.11
django-extensions>=3.2
django-axes>=6.0
bleach[css]>=6.0
django-csp>=4.0
httpx>=0.27
